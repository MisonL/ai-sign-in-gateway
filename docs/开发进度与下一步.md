# 开发进度与下一步

更新时间：2026-04-21

## 当前已完成

- 管理后台整体视觉已收敛为标准后台样式，去掉了多余模块小标题，信息层级更偏操作台。
- 后台壳层已调整为标准布局：
  左侧导航固定、顶部页头固定、右侧主区域内部滚动。
- 修复了页头高度导致的内容压入主区域问题，`a-layout-header` 已做强覆盖处理。
- 签到中心已补充签到配置：
  定时签到开关、签到时间、并发签到站点数、站点间隔秒数、仅执行启用站点。
- 后端系统设置已新增签到调度参数：
  `checkin_concurrency`、`checkin_interval_seconds`。
- 批量签到与计划签到已统一读取上述配置。
  默认行为为并发数 `1`、站点间隔 `1` 秒，即按每秒发起 1 个站点签到。
- 启动阶段已增加轻量运行时迁移，旧库会自动补齐 `system_settings` 新增字段。

## 本次数据库变更

- `system_settings.checkin_concurrency`
- `system_settings.checkin_interval_seconds`

## 当前验证情况

- 前端 `npm run build` 已通过。
- 后端 `go build ./...` 已通过；`go test ./...` 全绿。
- 当前仍存在前端构建产物体积偏大的告警，但不影响运行。

## 下一步建议

- 实机验证签到调度行为：
  重点确认“并发数”和“站点间隔”组合下的真实执行节奏是否符合预期。
- 增加签到任务的可观测性：
  建议记录批次开始时间、排队顺序、实际发起时间，方便排查节流与并发行为。
- 为数据库迁移补正式方案：
  当前是运行时补列，后续建议接入 Alembic 或固定迁移脚本。
- 优化前端打包体积：
  可优先考虑路由级拆包和 Ant Design Vue 按需加载。
- 增加回归验证：
  至少补签到中心、系统设置、站点中心的接口级和页面级测试。
