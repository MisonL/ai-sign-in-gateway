This directory is reserved for future runtime packaging experiments.

Current desktop builds use the `embedded_assets` build tag and generate
`cmd/ai-sign-in-gateway/embedded_dist/` temporarily from `frontend/dist`.
Use the scripts in `scripts/` instead of placing files here manually.
